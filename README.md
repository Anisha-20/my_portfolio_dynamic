#Code for creating dynamic portfolio website using HTML, CSS, JS for front-end, also Flask for backend (for rendering the pages).

#run this code inside virtual environment.
source portfolio-env/bin/activate
python app.py
